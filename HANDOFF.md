# はむあつめ — 引き継ぎ資料（Claude Code → Codex）

作成日: 2026-09-21　最終コミット: `4a77b2e`（main、origin と同期済み）

このファイルは「別のエージェント／人が今すぐ作業を引き継ぐ」ための資料。
恒常的なルールと仕様は [CLAUDE.md](CLAUDE.md) にあり、そちらも必ず読むこと。ここには **状況・失敗の経緯・次に何をすべきか** を書く。

---

## 1. 一言でいうと

- **ゲームとしては完成している**（5 画面、35 種のハムスター、16 グッズ、6 えさ、来訪シミュレーション、オフライン進行、セーブ、2 フレームアニメ、えさ皿 2 か所）。
- **絵だけがユーザーの満足に達していない。** ハムスターのドット絵を 6 回描き直して全部却下された。ここが引き継ぎの主題。
- 絵以外に既知のバグはない。

## 2. 技術スタック（変えないこと）

- 静的サイト、ビルドなし、依存ゼロ、ES modules 不使用（`file://` で開ける必要があるため）。
- `index.html` → `js/data.js` → `js/art.js` → `js/game.js` → `js/ui.js` → `js/main.js` の順にグローバル変数で読み込む。
- 絵はすべて **コードで生成したインライン SVG**（画像ファイルなし）。`js/art.js` の `Px` クラス（ピクセルキャンバス）に楕円・矩形を描き、色ごとの `<path>` にまとめて `shape-rendering: crispEdges` で出している。
- セーブは `localStorage`（`hamuatsume.save.v1`）。`game.js` の `normalize()` で旧形式（v0.1 の `food`）を `bowls[2]` に自動移行する。**これを壊さないこと。**

## 3. 絵の失敗の経緯（同じ失敗を繰り返さないために）

ユーザーの要望は一貫して **「ねこあつめ」「ひつじ村」の絵柄**。参考画像はねこあつめの猫一覧（正面座り、点目、ω 口、外に出るヒゲ）と、ひつじ村の牧場画面（3/4 俯瞰のドット絵）。

| # | 試した方向 | ユーザーの評価 | 学んだこと |
|---|---|---|---|
| 1 | SVG ベクター絵（太い輪郭、ベタ塗り） | 「気持ち悪すぎる」 | ベクター絵は却下。ドット絵が必須 |
| 2 | 20×20 の文字絵ドット | サイズ感が背景と不一致 | 密度をそろえないと安っぽい |
| 3 | 24×24 の文字絵（2×2 黒目・ほっぺ赤み・幅 6 の w 口） | 「可愛くなさすぎる」「気持ち悪い」 | 黒ベタの目と w 口は食パン／ヒキガエルに見える |
| 4 | 3 案提示（ミニマル／お団子／ハム太郎風） | 「全部ださい」 | 24 マスで何を配置しても Game Boy の解像度感が消えない |
| 5 | ねこあつめの顔記号（縦点目・ω・ヒゲ・マズル）を正面立ち絵に | 「全然ねこあつめっぽくない」「パースがおかしい」「刷新しろ」 | 背景が俯瞰なのに正面立ち絵は成立しない。記号を真似ても絵柄にはならない |
| 6 | 解像度 2 倍、3/4 俯瞰の「饅頭」にスリット目・ヒゲ・小さな肢 | 「気持ち悪い」 | 輪郭のない塊に細い目・ヒゲ・肢を付けると虫に見える |
| 7（現在） | 2 頭身、丸いビーズ目＋ハイライト、ほっぺ赤み、ヒゲなし | 未評価だが、ユーザーはこの時点で Codex への引き継ぎを決めた | 手続き生成の限界と判断すべき |

### 根本原因（引き継ぎ先への率直な見解）

- **「コードで楕円を組み合わせてドット絵を生成する」というアプローチ自体が限界。** ねこあつめの絵は人間のドット職人が 1 ピクセルずつ置いた絵であり、楕円の合成では「なんとなく丸い塊」以上にならない。
- 私（Claude Code）は描いた絵をスクリーンショットで確認はできるが、**ピクセル単位の審美的判断が弱い**。「可愛い／気持ち悪い」の差は数ピクセルの目の位置・輪郭の丸め方で決まり、そこを試行錯誤する能力が足りなかった。
- 6 回のイテレーションはすべて「前回の指摘を直す」形で進み、**絵柄の設計を最初に固めて合意を取る**工程を飛ばした。

### 推奨する打開策（優先順）

1. **本物のスプライト画像を使う**：ピクセルアーティストが描いた PNG スプライトシート（ハムスター 6 ポーズ × 2 フレーム、色は 1 枚のベースをパレット差し替え）を `img/` に置き、`art.js` の `hamster()` が `<img>` または `<canvas>` にパレットスワップして描く方式に切り替える。実在 35 種の色差はコード側のパレット置換で吸収できる（現在の `colors` データはそのまま使える）。無料素材（itch.io の CC0 動物スプライト）から始めて、気に入ったら発注する。
2. 手描きが無理なら、**画像生成 AI でねこあつめ風ハムスターを 1 枚作らせ、それをピクセル化してベース画像にする**。コードで形を作るより確実に「絵」になる。
3. コード生成を続けるなら、**解像度をさらに上げて（96×96 以上）アンチエイリアス付きのベクター寄りに戻す**。ただしユーザーは #1 でベクター絵を却下しているので、事前に小さなサンプルで合意を取ること。

いずれの場合も、**まず正面 1 ポーズ 1 種だけ作ってユーザーに見せ、OK が出てから 6 ポーズ × 35 種に展開する。** これを 6 回やらなかったのが最大の失敗。

## 4. 現在のコードで絵に関わる場所

| 場所 | 役割 |
|---|---|
| `js/art.js` 冒頭 `PX = 2.5` / `ANCHOR_PX = 5` | 1 グリッドの画面 px。ハムスター 48×48 = 120px、グッズ 88×72 = 220×180、皿 48×28、背景 512×256 |
| `js/art.js` `class Px` | ピクセルキャンバス。`ellipse/rect/line/trap`、`outline()`（自動輪郭）、`part()`（部品ごとに輪郭を付けて重ねる）、`paths()`（SVG 化） |
| `js/art.js` `DRAW[pose](p, c, frame, tail)` | ハムスター 6 ポーズ。`bodyFront()` を front/eat/back で共用 |
| `js/art.js` `HEAD` / `ACC` | レアはむ 6 種のアクセサリー（王冠・輪っか・はちまき・タオル・バケツ・ひげ）の位置と文字絵 |
| `js/art.js` `hamster(opts)` | 公開 API。`{ colors, pose, flip, size, silhouette, accessory, tail, className, anim }` → SVG 文字列。**呼び出し側（ui.js）はこの API だけに依存している**ので、中身を PNG 方式に差し替えても ui.js は無変更で済む |
| `js/art.js` `ITEM_DRAW` / `item()` / `bowl()` | グッズ 16 種とえさ皿。44×36 座標で描いて内部で 2 倍 |
| `js/art.js` `room()` | おへや背景（柵・木・小道・花）。これはユーザーから不満が出ていない |
| `js/data.js` `HAMSTERS[].colors` | 種ごとの色 `{ body, belly, ear?, stripe?, spots?, brow?, face?, eye?, tail? }` |
| `js/data.js` `ITEMS[].anchor` / `BOWLS[].anchor` | ハムスターの足元中央を置く位置（グッズは 44×36、皿は 24×14 のグリッド座標） |
| `css/style.css` `.hamu.anim` 周辺 | 2 フレームアニメ。`.f1/.f2` を CSS `steps(1)` で切り替え（`anim-alt` 交互、`anim-blink` たまに）。JS タイマーなし |
| `design/sprites.html` | 全ポーズ・全種・全グッズ・皿・背景を一覧する確認ページ（`?sec=poses|all|items|bowls|room`）。**絵を触ったら必ずここで確認する** |

## 5. 動かし方・確認の仕方

```bash
# そのまま開ける（Google Fonts 以外は file:// で動く）
start index.html

# HTTP で配信したい場合
python -m http.server 8791 --bind 127.0.0.1
# → http://localhost:8791/index.html, http://localhost:8791/design/sprites.html?sec=poses
```

- ブラウザは古い `js/art.js` をキャッシュしがち。変更が反映されないときは Ctrl+F5。
- DevTools で住人を即座に置く：
  ```js
  const g=hamu.game,D=HamuData;
  ['wheel','house','tunnel'].forEach((id,i)=>{g.state.inventory[id]=1;g.placeItem('s'+(i+1),id)});
  g.setFood('worm',1);
  g.freeSeats().slice(0,5).forEach((st,i)=>g.arrive(D.HAMSTERS[i],st,{arrivals:0,newHams:[]}));
  hamu.ui.render();
  ```
- せってい → 時間の速さ ×60 で数十秒に 1 回来訪判定が走る。
- この環境には `node` / `gh` がなかった。Codex 側で Playwright が使えるなら `pageerror` が出ないことを自動確認すると良い。

## 6. Git

- リモート: `https://github.com/riku1128-tong/hamuatsume.git`、ブランチは `main` のみ。ブランチ・PR は作らず main に直接 push する運用。
- リポジトリ限定で `user.name = riku1128-tong` / `user.email = riku1128@gmail.com` を設定済み（`--global` ではない）。
- コミットメッセージは日本語。小さくコミット。
- ユーザーはリポジトリをプライベートにしたい意向（`gh` がなく未対応。GitHub の Settings → Danger Zone から手動で）。

## 7. 絵以外の未実装・改善候補（優先順、ユーザー未指示）

1. グッズ側のアニメーション（回し車が回る等）
2. 効果音・BGM（設定でオンオフ）
3. レアはむ追加・季節イベント
4. 図鑑コンプリート報酬・実績
5. GitHub Pages 公開
6. 英語化

## 8. ユーザーについて

- 日本語話者。指示は短く率直（「ださい」「気持ち悪い」「刷新しろ」）。**方向性を先に見せて合意を取ってから量産すること**を強く望んでいる（「ちょっと案を先に出して。方向性をあわせたい」と明示された）。
- 参考にしている作品：ねこあつめ（キャラの絵柄）、ひつじ村（画面全体の俯瞰ドット絵）。
- 「px 単位で修正できる次元じゃない」と言われたら、微調整ではなく作り直しが求められている。
